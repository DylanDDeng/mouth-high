# Mouth High

🎙️ A voice-powered writing assistant that turns your speech into text instantly. Features hands-free recording modes, real-time audio visualization, customizable shortcuts, and local history management.

一款语音输入工具，使用阿里云 DashScope 通义千问 ASR 服务进行语音识别。

## 功能特性

### 🎤 语音输入
- **双模式录音** - 按住模式（按住说话，松开识别）或切换模式（按一下开始，再按一下停止）
- **实时波形显示** - Toggle 模式下显示浮动录音波纹条，直观反馈录音状态
- **物理键盘录制** - 直接在键盘上按下想要的组合键即可设置新快捷键

### ⚡ 快捷操作
- **全局快捷键** - 默认 `Cmd + R`（可自定义），任何应用中都可用
- **自定义快捷键** - 支持任意修饰键组合（Cmd/Ctrl/Shift/Alt + 字母/功能键）
- **快速设置** - 左侧边栏一键进入设置

### 📝 输出方式
- **键盘输入** - 直接模拟键盘输入到当前光标位置
- **剪贴板** - 复制到剪贴板并自动粘贴

### 💾 数据管理
- **历史记录** - 自动保存转录历史，支持搜索和删除
- **本地存储** - 所有数据仅保存在本地，保护隐私
- **保留策略** - 可设置历史记录保留时间（7天/30天/90天/永久）

## 系统要求

- macOS（Apple Silicon，M 系列芯片）
- 网络连接（用于调用 DashScope API）
- 麦克风权限
- 辅助功能权限（用于模拟键盘输入）

## 安装

### 1. 安装 Python 依赖

```bash
cd src-python
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 2. 配置 API Key

首次运行需要在设置中配置阿里云 DashScope API Key。

获取方式：
1. 访问 [阿里云 DashScope](https://dashscope.aliyun.com/)
2. 开通语音识别服务
3. 创建 API Key
4. 在应用设置中粘贴

### 3. 安装 Node.js 依赖

```bash
npm install
```

### 4. 运行开发版本

```bash
npm run tauri dev
```

### 5. 构建发布版本

```bash
npm run tauri build
```

## 使用方法

### 快捷上手

1. 启动应用，完成 API Key 配置
2. 在任意应用的光标位置，按住 `Cmd + R`（或你设置的快捷键）
3. 开始说话
4. 松开按键（Hold 模式）或点击波纹条确认（Toggle 模式）
5. 文字自动输入到光标位置

### 切换录音模式

进入**设置** > **录音模式**：
- **按住模式**：按住快捷键录音，松开自动停止并识别
- **切换模式**：按一下开始录音，屏幕底部出现波纹条，再按一下或点击波纹条完成识别

### 设置快捷键

进入**设置** > **快捷键**：
1. 点击"点击记录新快捷键"
2. 在键盘上按下想要的组合键
3. 点击确认保存

## 权限设置

首次运行时需要授权以下权限：

1. **麦克风权限** - 用于录制语音
2. **辅助功能权限** - 用于模拟键盘输入（系统设置 > 隐私与安全性 > 辅助功能）

## 项目结构

```
mouth-high/
├── src/                          # 前端 React 代码
│   ├── App.tsx                   # 主应用组件
│   ├── components/
│   │   ├── History.tsx           # 历史记录页面
│   │   ├── SettingsPage.tsx      # 设置页面
│   │   ├── HotkeyRecorder.tsx    # 快捷键录制组件
│   │   └── RecordingBarWindow.tsx # 录音波纹条窗口
│   ├── styles.css                # 全局样式
│   ├── recording-bar-main.tsx    # 录音条窗口入口
│   └── recording-bar.css         # 录音条样式
├── src-tauri/                    # Rust 后端代码
│   ├── src/
│   │   ├── lib.rs                # 主逻辑
│   │   ├── audio.rs              # 音频录制
│   │   ├── hotkey.rs             # 全局快捷键
│   │   ├── input.rs              # 键盘/剪贴板输出
│   │   ├── focus.rs              # 焦点管理
│   │   └── sidecar.rs            # Python 进程通信
│   └── Cargo.toml
├── src-python/                   # Python ASR 服务
│   ├── asr_service.py            # 语音识别服务
│   └── requirements.txt
└── package.json
```

## 技术栈

- **前端**: React + TypeScript + Vite
- **后端**: Tauri (Rust)
- **ASR**: 阿里云 DashScope 通义千问 ASR
- **UI**: 米色主题设计

## License

MIT
